title = "gitleaks config"

# Start from gitleaks' built-in ruleset; add project-specific allowlist
# entries here as false positives come up (e.g. test fixtures).
[extend]
useDefault = true

[allowlist]
description = "Global allowlist"
paths = [
  '''package-lock\.json''',
]
