# docs/

- Update `docs/INDEX.md` in the same change that adds, moves, or removes a doc — a stale index is worse than none, since it's trusted.
- Keep `docs/architecture/overview.md` and `docs/testing.md` describing actual current behavior, not aspirational plans.
