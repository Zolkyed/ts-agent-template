# src/

- Every export needs a corresponding test at the mirrored path in `test/`.
- `src/index.ts` is the package's public API — only re-export what's meant to be consumed from outside this package.
