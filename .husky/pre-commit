npx lint-staged

if command -v gitleaks >/dev/null 2>&1; then
  gitleaks protect --staged --redact -v
else
  echo "gitleaks not installed locally — skipping local secret scan (CI still runs it). See docs/guides/setup.md." >&2
fi
