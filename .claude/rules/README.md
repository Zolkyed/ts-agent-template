# Rules

Add a `.md` file here to define a rule scoped by file glob rather than directory — it's auto-loaded only when a matching file is touched, regardless of where in the repo that file lives. Complements nested `AGENTS.md` files, which are directory-scoped instead.
